//A finir

var arreglo = [
    {
        name: "cristian",
        value: 4000
    },
    {
        name: "Jean",
        value: 3500
    }
]

var sueldo = 0;
var empleos_name = "";

for (let [key, value] of Object.entries(empleos_sueldos)) {
    console.log(Object.keys(empleos_sueldos));
    if ( empleos_sueldos[i] > sueldo) {
        sueldo = empleos_sueldos[i];
        empleos_name = empleos_sueldos
    };
    document.write(`${key}: ${value}`);
};

document.write("El empleo que gagna mas se llama " + " y gagna " + sueldo);
